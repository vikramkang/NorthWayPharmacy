// Environment-specific config. Change this per deployment (local/staging/prod).
// Kept as a separate file so it's the one line anyone needs to touch —
// no rebuilding or regenerating the site required.
window.NORTHWAY_API_BASE = "http://localhost:4000";
